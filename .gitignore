# dependencies（仅应用自身的依赖目录，避免 542MB 进 git；内置 Node 运行时整目录由下方 desktop/node/ 管理）
desktop/node_modules/

# 内置 DSH 运行时（245MB，npm install 生成，不进 git；package.json 保留）
desktop/dsh-runtime/node_modules/
desktop/dsh-runtime/install.log
desktop/dsh-runtime/package-lock.json

# logs
*.log

# OS
Thumbs.db
.DS_Store

# desktop build output (installer / unpacked app)
desktop/release/
desktop/install.log
desktop/build.log
desktop/node/

# 敏感：凭证 / 设置 / 本地状态（绝不能提交到仓库）
.credentials.yaml
settings.yaml

# 运行中的便携实例数据（会话/缓存，不入库）
desktop/dsh/data/
desktop/dsh/portable.dat

# 便携运行实例整目录（desktop/dsh：数据+内置运行时+资源，均不入库；源码在 desktop/gate、desktop/plugin-static 等）
desktop/dsh/

# 测试环境运行时数据（脚本保留，home 数据不入库）
test-env/home/
test-env/t*-out.log
test-env/t*-err.log
test-env/v*-out.log
test-env/v*-err.log

# 备份目录（含会话记录，敏感，不入库）
BACKUP/
watchdog.log
restart-test.log

dsh-client-*.json
dsh-market.json
.env
.env.*
*.local

# 隔离测试环境（运行时数据，不进 git）
desktop/test-env/.dsh/
desktop/test-env/app/
desktop/test-env/out.log
desktop/test-env/err.log
desktop/test-env/pid.txt

# Tailscale APK 已上传 GitHub release，不入库
mobile/tailscale-android.apk
